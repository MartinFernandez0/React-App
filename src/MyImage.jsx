// eslint-disable-next-line no-unused-vars
function MyImage({ imageUrl}) {
    return (
        <div>
            <img src={imageUrl} alt="IMG Dragon Ball Z" />
        </div>
    );
}

export default MyImage;
